# migrations

Aurora PostgreSQL schema migrations, including the **ledger genesis-singleton** migration and
the **ledger unique-key idempotency constraint** (authoritative exactly-once for postings).

