# `notifications/preferences`

Per-Individual per-trigger channel toggles, timezone-aware Quiet Hours with critical bypass, digest frequency.

---
Part of the `notifications` bounded context. ESLint enforces the sub-boundaries (see `docs/ARCHITECTURE.md`).

