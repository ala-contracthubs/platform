# `notifications/trigger-registry`

Declarative typed triggers registered via Module Manifests; locale-keyed versioned templates; contract-tested.

---
Part of the `notifications` bounded context. ESLint enforces the sub-boundaries (see `docs/ARCHITECTURE.md`).

