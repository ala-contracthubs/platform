# `notifications/storage-quota`

Per-Individual cross-Case byte quota that blocks media at the limit but never blocks text.

---
Part of the `notifications` bounded context. ESLint enforces the sub-boundaries (see `docs/ARCHITECTURE.md`).

