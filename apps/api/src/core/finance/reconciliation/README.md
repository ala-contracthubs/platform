# `finance/reconciliation`

Daily Wallet + Bookkeeping invariant checks, drift queue, Platform-Admin financial dashboard read model. CLIENT of `LedgerWritePort`.

---
Part of the `finance` bounded context. ESLint enforces the sub-boundaries (see `docs/ARCHITECTURE.md`).

