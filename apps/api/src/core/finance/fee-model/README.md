# `finance/fee-model`

Fixed / Tiered / Percentage Fee Models, Settlement Mode, Base Fee split, Top-up + Top-up Cap, per-Agency overrides, Manifest versioning.

---
Part of the `finance` bounded context. ESLint enforces the sub-boundaries (see `docs/ARCHITECTURE.md`).

