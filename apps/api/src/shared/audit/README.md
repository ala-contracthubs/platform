# shared/audit

append-only tamper-evident AuditEntry actor/timestamp/before-after; retention policy + messaging/compliance export; infrastructure/application-layer, NEVER imported by domain — audit writes are an application/event-handler responsibility
