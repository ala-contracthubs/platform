// apps/web ESLint config. Base preset only for now — the same flat peer-module
// boundaries can be layered in (from @contracthubs/config/eslint/boundaries)
// once the web app grows more modules.
import base from '@contracthubs/config/eslint'

export default [...base]
