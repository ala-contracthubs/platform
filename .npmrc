# pnpm configuration for the ContractHubs platform monorepo
engine-strict=true
strict-peer-dependencies=false
auto-install-peers=true
save-workspace-protocol=rolling
# keep node_modules isolated so the ESLint module boundaries can't be bypassed via hoisting
shamefully-hoist=false
